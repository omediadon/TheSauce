<section class="testimonials text-center bg-light">
    <div class="container">
        <h2 class="mb-5">What people are saying...</h2>
        <div class="row">
            {% for testemonial in testemonials %}
                {% include "parts/testemonial.twig" with {"testemonial":testemonial} only %}
            {% endfor %}
        </div>
    </div>
</section>
